This notebook (cPHA-10030_WS2_-_Linear_regression.ipynb) reads `protein_assay.csv`
from the same folder it's in.

That CSV was never uploaded to me, so I couldn't include it — you'll need to add
`protein_assay.csv` into this `content/` folder yourself before pushing.

Delete this file once you've added the real data file — it's just a placeholder
reminder, not something that needs to ship in the live site.
